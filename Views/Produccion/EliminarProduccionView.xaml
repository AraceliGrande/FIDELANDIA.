<UserControl x:Class="FIDELANDIA.Views.Produccion.EliminarProduccionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Encabezado -->
            <RowDefinition Height="*"/>
            <!-- Contenido -->
            <RowDefinition Height="Auto"/>
            <!-- Botones -->
        </Grid.RowDefinitions>

        <!-- 🏭 Encabezado -->
        <Border Grid.Row="0" Background="White" Padding="24" BorderBrush="#DDD" BorderThickness="0,0,0,1">
            <TextBlock Text="🏭 Eliminar producción" 
                       FontSize="20" FontWeight="Bold" 
                       Foreground="#E5740A"/>
        </Border>

        <!-- Contenido central -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="32,24" HorizontalAlignment="Stretch">
                <!-- 🧩 Detalle del lote -->
                <Border BorderBrush="#EEE" BorderThickness="1" CornerRadius="6" Padding="20" Background="White">
                    <Grid HorizontalAlignment="Center">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="ID Lote:" Grid.Row="0" Grid.Column="0" FontWeight="SemiBold" Foreground="#444"/>
                        <TextBlock Text="{Binding IdLote}" Grid.Row="0" Grid.Column="1"/>

                        <TextBlock Text="Tipo de Pasta:" Grid.Row="1" Grid.Column="0" FontWeight="SemiBold" Foreground="#444"/>
                        <TextBlock Text="{Binding TipoPasta}" Grid.Row="1" Grid.Column="1"/>

                        <TextBlock Text="Cantidad Producida:" Grid.Row="2" Grid.Column="0" FontWeight="SemiBold" Foreground="#444"/>
                        <TextBlock Text="{Binding CantidadProduccion, StringFormat={}{0:N2} envases}" Grid.Row="2" Grid.Column="1"/>

                        <TextBlock Text="Fecha de Producción:" Grid.Row="3" Grid.Column="0" FontWeight="SemiBold" Foreground="#444"/>
                        <TextBlock Text="{Binding FechaProduccion, StringFormat={}{0:dd/MM/yyyy}}" Grid.Row="3" Grid.Column="1"/>

                        <TextBlock Text="Stock Actual:" Grid.Row="4" Grid.Column="0" FontWeight="SemiBold" Foreground="#444"/>
                        <TextBlock Text="{Binding Stock, StringFormat={}{0:N2} envases}" Grid.Row="4" Grid.Column="1"/>
                    </Grid>
                </Border>

                <!-- 🧾 Detalles de venta asociados -->
                <Border BorderBrush="#DDD" CornerRadius="6" Padding="20" Background="White" Margin="0,24,0,0">
                    <StackPanel>
                        <TextBlock Text="🧾 Detalles de venta asociados al lote" FontWeight="SemiBold" FontSize="12" Foreground="#333" Margin="0,0,0,12"/>

                        <ItemsControl ItemsSource="{Binding VentasAsociadas}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#EEE" BorderThickness="1" CornerRadius="6" Padding="16" Margin="0,0,0,10" Background="#FAFAFA">
                                        <Grid HorizontalAlignment="Center">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="180"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Text="ID Lote:" Grid.Row="2" Grid.Column="0" FontWeight="SemiBold" Foreground="#444"/>
                                            <TextBlock Text="{Binding IdLote}" Grid.Row="2" Grid.Column="1" Foreground="#222"/>

                                            <TextBlock Text="ID Venta:" Grid.Row="1" Grid.Column="0" FontWeight="SemiBold" Foreground="#444"/>
                                            <TextBlock Text="{Binding IdVenta}" Grid.Row="1" Grid.Column="1" Foreground="#222"/>

                                            <TextBlock Text="Cantidad vendida:" Grid.Row="3" Grid.Column="0" FontWeight="SemiBold" Foreground="#444"/>
                                            <TextBlock Text="{Binding Cantidad, StringFormat={}{0:N2} kg}" Grid.Row="3" Grid.Column="1" Foreground="#222"/>

                                            <TextBlock Text="Fecha de venta:" Grid.Row="5" Grid.Column="0" FontWeight="SemiBold" Foreground="#444"/>
                                            <TextBlock Text="{Binding Fecha, StringFormat={}{0:dd/MM/yyyy}}" Grid.Row="5" Grid.Column="1" Foreground="#222"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Mensaje si no hay detalles -->
                        <TextBlock Text="No hay detalles de venta asociados a este lote."
                               Foreground="Gray" FontStyle="Italic" FontSize="12"
                               HorizontalAlignment="Center" Margin="0,12,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding VentasAsociadas.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- Botones inferiores -->
        <Border Grid.Row="2" Background="White" Padding="16">
            <StackPanel Orientation="Vertical" Margin="0">

                <!-- ⚠️ Advertencia -->
                <Border Background="#FFF3CD" BorderBrush="#FFEBA1" BorderThickness="1" CornerRadius="6" Padding="16" Margin="2">
                    <StackPanel>
                        <TextBlock Text="⚠️ Advertencia" FontWeight="Bold" Foreground="#856404" FontSize="11" Margin="0,0,0,8"/>
                        <TextBlock Text="Esta acción eliminará el lote de producción seleccionado y las ventas asociadas a dicho lote que se meustran en la pantalla" TextWrapping="Wrap" Foreground="#856404" FontSize="10" Margin="25,0,0,2"/>
                        <TextBlock Text="El stock del tipo de pasta correspondiente se ajustará restando la cantidad producida de este lote." TextWrapping="Wrap" Foreground="#856404" FontSize="10" Margin="25,0,0,2"/>
                    </StackPanel>
                </Border>

                <!-- Botones -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button x:Name="BtnCancelar" Content="Cancelar" Width="150" Height="36" Background="DimGray" Foreground="White" FontSize="12" BorderBrush="#DDD" FontWeight="SemiBold" Cursor="Hand" Margin="0,0,8,0" Click="BtnCancelar_Click"/>
                    <Button x:Name="BtnEliminar" Content="🗑️ Eliminar producción" Width="180" Height="36" FontSize="12" FontWeight="Bold" Background="#E74C3C" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnEliminar_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#E74C3C"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#C0392B"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

            </StackPanel>
        </Border>

    </Grid>
</UserControl>
