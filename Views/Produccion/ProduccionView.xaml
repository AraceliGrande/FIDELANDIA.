<UserControl x:Class="FIDELANDIA.Views.Produccion.ProduccionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <Grid Margin="20">
        <Border Background="White" CornerRadius="12" Padding="16" 
                BorderBrush="#eee" BorderThickness="1" Effect="{StaticResource DropShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <!-- Encabezado -->
                    <RowDefinition Height="Auto"/>
                    <!-- Contenido scroll -->
                    <RowDefinition Height="*"/>
                    <!-- Footer fijo -->
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header con título y botones principales -->
                <DockPanel Grid.Row="0" Margin="0,0,0,16">
                    <TextBlock Text="Producciones Disponibles" 
                               FontSize="20" FontWeight="Bold" 
                               Foreground="#b65601"
                               VerticalAlignment="Center"
                               DockPanel.Dock="Left"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="➕ Nuevo lote de producción" 
                                Style="{StaticResource SecondaryButton}" 
                                Height="29" Width="205" Margin="0,0,10,0"
                                Click="NuevoLote_Click"/>

                            <Button Content="💰 Nueva venta de productos" 
                                Style="{StaticResource SecondaryButton}" 
                                Height="29" Width="205"
                                Click="NuevaVenta_Click"/>

                        </StackPanel>
                    </StackPanel>
                </DockPanel>

                <!-- Contenido scrollable -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="2" Margin="20"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="10" Margin="10">
                                    <StackPanel>
                                        <!-- Título de la sección -->
                                        <Border CornerRadius="4" Padding="6" Margin="0,0,0,10">
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <Ellipse Width="12" Height="12" Fill="#78E362" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                                <TextBlock Text="{Binding Nombre}" FontSize="14" FontWeight="Bold" Foreground="#1F6412" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>

                                        <!-- Tabla -->
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Encabezados -->
                                            <TextBlock Text="Nº Lote" FontWeight="Bold" Grid.Row="0" Grid.Column="0"/>
                                            <TextBlock Text="Fecha producción" FontWeight="Bold" Grid.Row="0" Grid.Column="1"/>
                                            <TextBlock Text="Cantidad (kg)" FontWeight="Bold" Grid.Row="0" Grid.Column="2"/>
                                            <TextBlock Text="Paquetes" FontWeight="Bold" Grid.Row="0" Grid.Column="3"/>

                                            <!-- Filas dinámicas -->
                                            <ItemsControl ItemsSource="{Binding Filas}" Grid.Row="1" Grid.ColumnSpan="4">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Margin="0,0,0,5">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="*"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Text="{Binding [0]}" Grid.Column="0"/>
                                                            <TextBlock Text="{Binding [1]}" Grid.Column="1"/>
                                                            <TextBlock Text="{Binding [2]}" Grid.Column="2"/>
                                                            <TextBlock Text="{Binding [3]}" Grid.Column="3"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Footer fijo -->
                <Grid Grid.Row="2" Margin="0,20,20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Botones del resumen -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="📊 Generar balance diario" Style="{StaticResource PrimaryButton}" Height="30" Width="200" Margin="10,0"/> 
                        <Button Content="💰 Resumen de venta diario" Style="{StaticResource PrimaryButton}" Height="30" Width="200" Margin="10,0"/>
                        <Button Content="🍳 Resumen de producción diario" Style="{StaticResource PrimaryButton}" Height="30" Width="200" Margin="10,0"/>
                    </StackPanel>

                    <!-- Botón flotante circular -->
                    <Button Grid.Column="1"
                            Content="＋"
                            Style="{StaticResource SecondaryButton}"
                            Width="35" Height="35" Margin="10"
                            FontSize="15"
                            FontWeight="Bold"
                            Background="White"
                            Foreground="#333"
                            BorderBrush="#ccc"
                            BorderThickness="1"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Click="BtnNuevoTipoPasta_Click">    
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="50"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Padding="5"
                                        Effect="{DynamicResource DropShadowEffect}">    
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
