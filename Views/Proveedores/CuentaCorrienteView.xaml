<UserControl x:Class="FIDELANDIA.Views.CuentaCorrienteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="900">

    <Border Background="White" CornerRadius="12" Padding="20"
            BorderBrush="#eee" BorderThickness="1" Effect="{StaticResource DropShadow}">

        <DockPanel LastChildFill="True">

            <!-- Botones abajo -->
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="➕ Nuevo Movimiento" Style="{StaticResource PrimaryButton}" 
                        Margin="0,0,12,0" x:Name="BtnNuevoMovimiento" Width="165" Height="32"
                        Click="BtnNuevaTransaccion_Click"/>
                <Button Content="⬇ Exportar CSV" Style="{StaticResource SecondaryButton}" Height="32" Width="163"/>
            </StackPanel>

            <StackPanel>

                <!-- ENCABEZADO - Datos del proveedor -->
                <Border Background="#f9f9f9" CornerRadius="8" Padding="16" BorderBrush="#ddd" BorderThickness="1" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Info básica -->
                        <StackPanel>
                            <TextBlock Text="Cuenta Corriente" FontSize="14" Foreground="#a34c00" FontWeight="SemiBold"/>
                            <TextBlock x:Name="DetalleNombre" Text="Proveedor 1" FontSize="20" FontWeight="Bold" Margin="0,4,0,12" Foreground="#333"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="📋 CUIT:" FontWeight="Bold" Grid.Column="0" Margin="0,0,8,0"/>
                                <TextBlock x:Name="DetalleCuit" Text="00-00000000-0" Grid.Column="1" Foreground="#555"/>
                            </Grid>

                            <Grid Margin="0,4,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="📞 Teléfono:" FontWeight="Bold" Grid.Column="0" Margin="0,0,8,0"/>
                                <TextBlock x:Name="DetalleTelefono" Text="(011) 1234-5678" Grid.Column="1" Foreground="#555"/>
                            </Grid>

                            <Grid Margin="0,4,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="📧 Email:" FontWeight="Bold" Grid.Column="0" Margin="0,0,8,0"/>
                                <TextBlock x:Name="DetalleEmail" Text="proveedor@mail.com" Grid.Column="1" Foreground="#555"/>
                            </Grid>
                        </StackPanel>

                        <!-- Total / saldo -->
                        <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <TextBlock Text="Saldo actual" FontSize="13" Foreground="#777" TextAlignment="Right"/>
                            <TextBlock x:Name="DetalleSaldo" Text="$ 0,00" FontSize="24" FontWeight="Bold" Foreground="#2e7d32" TextAlignment="Right"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- FILTROS -->
                <Border Background="#fefefe" CornerRadius="8" Padding="10" BorderBrush="#ddd" BorderThickness="1" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Desde -->
                        <Grid Grid.Column="0" Margin="0,0,8,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Desde:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <Border Grid.Column="1" CornerRadius="6" BorderBrush="#ccc" BorderThickness="1">
                                <DatePicker HorizontalAlignment="Stretch" Margin="2"/>
                            </Border>
                        </Grid>

                        <!-- Hasta -->
                        <Grid Grid.Column="1" Margin="0,0,8,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Hasta:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <Border Grid.Column="1" CornerRadius="6" BorderBrush="#ccc" BorderThickness="1">
                                <DatePicker HorizontalAlignment="Stretch" Margin="2"/>
                            </Border>
                        </Grid>

                        <!-- Botón Buscar -->
                        <Button Content="Buscar" Style="{StaticResource PrimaryButton}" Grid.Column="2" Width="100" Height="28"/>
                    </Grid>
                </Border>

                <!-- TABLA DE MOVIMIENTOS -->
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <DataGrid x:Name="MovimientosGrid" AutoGenerateColumns="False" CanUserAddRows="False"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal" RowHeight="28">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha}" Width="80"/>
                            <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="80"/>
                            <DataGridTextColumn Header="Concepto" Binding="{Binding Concepto}" Width="*"/>
                            <DataGridTextColumn Header="Debe" Binding="{Binding Debe}" Width="80" ElementStyle="{StaticResource RightAlignStyle}"/>
                            <DataGridTextColumn Header="Haber" Binding="{Binding Haber}" Width="80" ElementStyle="{StaticResource RightAlignStyle}"/>
                            <DataGridTextColumn Header="Saldo" Binding="{Binding Saldo}" Width="80" ElementStyle="{StaticResource RightAlignStyle}"/>
                            <DataGridTextColumn Header="Factura" Binding="{Binding Factura}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </ScrollViewer>
            </StackPanel>
        </DockPanel>
    </Border>
</UserControl>
