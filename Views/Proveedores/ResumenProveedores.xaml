<UserControl x:Class="FIDELANDIA.Views.Proveedores.ResumenProveedores"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" Margin="10">
        <DataGrid x:Name="ResumenDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  RowHeight="30"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="#F8F8F8"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#DDD"
                  VerticalGridLinesBrush="#DDD">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Proveedor" Binding="{Binding Nombre}" Width="200"/>
                <DataGridTextColumn Header="CUIT" Binding="{Binding Cuit}" Width="120"/>
                <DataGridTextColumn Header="Saldo Actual" Binding="{Binding SaldoActual, StringFormat=C2}" Width="120"/>
                <DataGridTextColumn Header="Límite Crédito" Binding="{Binding LimiteCredito, StringFormat=C2}" Width="120"/>
                <DataGridTextColumn Header="Última Transacción" Binding="{Binding UltimaTransaccionInfo}" Width="*"/>
                <DataGridTextColumn Header="Cantidad Movimientos" Binding="{Binding TotalTransacciones}" Width="120"/>
                <DataGridTextColumn Header="Activo" Binding="{Binding IsActivo}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
